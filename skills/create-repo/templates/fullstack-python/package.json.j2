{
  "name": "{{ project_name }}",
  "private": true,
  "scripts": {
    "lint:web": "biome check --error-on-warnings apps/web/",
    "lint:web:fix": "biome check --write apps/web/"
  },
  "devDependencies": {
    "@biomejs/biome": "{{ versions.biomejs_biome }}"
  },
  "packageManager": "pnpm@{{ versions.pnpm }}"
}
