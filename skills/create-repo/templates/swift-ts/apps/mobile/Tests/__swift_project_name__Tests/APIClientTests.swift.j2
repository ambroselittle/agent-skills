import Testing
@testable import {{ swift_project_name }}

@Suite("APIClient")
struct APIClientTests {
    @Test("baseURL defaults to localhost")
    func defaultBaseURL() async {
        let client = APIClient()
        let url = await client.baseURL
        #expect(url.absoluteString == "http://localhost:3001/api")
    }
}
