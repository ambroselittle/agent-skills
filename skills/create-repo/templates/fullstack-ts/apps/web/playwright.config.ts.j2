import { defineConfig, devices } from "@playwright/test"

const webPort = Number(process.env.E2E_WEB_PORT) || 4100
const apiPort = Number(process.env.E2E_API_PORT) || 4101

export default defineConfig({
  testDir: "./e2e",
  retries: 0,
  use: {
    baseURL: `http://localhost:${webPort}`,
    trace: "retain-on-failure",
  },
  projects: [
    {
      name: "chromium",
      use: { ...devices["Desktop Chrome"] },
    },
  ],
  webServer: [
    {
      command: `PORT=${apiPort} pnpm --filter {{ scope }}/api run dev`,
      port: apiPort,
      reuseExistingServer: false,
      cwd: "../..",
    },
    {
      command: `VITE_API_PORT=${apiPort} npx vite --port ${webPort}`,
      port: webPort,
      reuseExistingServer: false,
    },
  ],
})
