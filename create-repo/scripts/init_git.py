"""Initialize a git repo and optionally push to GitHub."""


def init_git(
    project_dir: str,
    project_name: str,
    template_name: str,
    stack_description: str,
    *,
    no_github: bool = False,
) -> None:
    """Create initial commit and optionally create a GitHub repo."""
    pass
