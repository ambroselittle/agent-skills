"""Scaffold a project from a Jinja2 template."""


def scaffold(
    project_name: str,
    template_name: str,
    versions_json_path: str,
    output_dir: str,
) -> None:
    """Render template files and write to output_dir."""
    pass
