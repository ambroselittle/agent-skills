{
  "hooks": {
    "PreToolUse": {
      "rules": [
        {
          "rule": "block-env-reads",
          "allowedPaths": [".eval-runs/**/.env*", ".eval-runs/**/.envrc"]
        }
      ]
    }
  }
}
